#include<stdio.h>
main()
{
int a=0x41;   //Hexadecial numbers into decimal value(x->hex(41=
int b=010;    // Octal numbers means we want to put ZERO(1) in FIRST
float c=112.25;
printf("%d",a);
printf("%0.2f",c); // Here (0.2) is used to given the output what we given in input statement (if not give (0.2) means it will execute 1.25 as = 1.250000 So we give (0.2))
}
